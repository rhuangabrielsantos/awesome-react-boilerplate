import { Meta, StoryObj } from '@storybook/react';
import { {{pascalCase name}}Component } from './{{pascalCase name}}Component';

type Story = StoryObj<typeof {{pascalCase name}}Component>;

const meta: Meta<typeof {{pascalCase name}}Component> = {
  title: 'Components/{{pascalCase name}}Component',
  component: {{pascalCase name}}Component
};

export const Basic: Story = {};

export default meta;
